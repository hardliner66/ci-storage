name: "CI Storage - Init"
description: "Initialize CI storage system if needed"
author: "hardliner66"

runs:
    using: "composite"
    steps:
        - name: Run ci-storage init
          shell: bash
          run: |
              SCRIPT="${GITHUB_ACTION_PATH}/../ci-storage"

              chmod +x "$SCRIPT"
              export CI_STORAGE_REPO_ROOT=${{ github.workspace }}
              bash "$SCRIPT" init
